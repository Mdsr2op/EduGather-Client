/**
 * Global configuration settings for video meetings
 */

// Maximum meeting duration in seconds (default: 1 hour)
export const MAX_MEETING_DURATION_SECONDS = 3600;

// Maximum meeting duration in milliseconds (for easier use with Date calculations)
export const MAX_MEETING_DURATION_MS = MAX_MEETING_DURATION_SECONDS * 1000; 